# Default Environment Variables (Local Development)
# This file is used by default when running docker-compose for local development

ENV=local
SPRING_PROFILES_ACTIVE=local

# Database Configuration
POSTGRES_DB=identity_server_local_db
POSTGRES_USER=sa
POSTGRES_PASSWORD=123456
POSTGRES_PORT=5432

# Backend Configuration
BACKEND_PORT=8080
